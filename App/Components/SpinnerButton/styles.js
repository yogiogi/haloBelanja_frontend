import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  spinner: {
    justifyContent: 'center',
    alignItems: 'center',
    width: '100%',
    height: '100%',
    zIndex: 4,
  },
});
